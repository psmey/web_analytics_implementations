/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { IDsOptimizationId } from '../../models/i-ds-optimization-id';
import { OutgoingOptimizationStateDefinition } from '../../models/outgoing-optimization-state-definition';

export interface OptimizationGetOptimizationState$Params {

/**
 * Parameter within the path for the id of an optimization.
 */
  optimizationId: IDsOptimizationId;
}

export function optimizationGetOptimizationState(http: HttpClient, rootUrl: string, params: OptimizationGetOptimizationState$Params, context?: HttpContext): Observable<StrictHttpResponse<OutgoingOptimizationStateDefinition>> {
  const rb = new RequestBuilder(rootUrl, optimizationGetOptimizationState.PATH, 'get');
  if (params) {
    rb.path('optimizationId', params.optimizationId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OutgoingOptimizationStateDefinition>;
    })
  );
}

optimizationGetOptimizationState.PATH = '/v1/optimizations/{optimizationId}/state';
